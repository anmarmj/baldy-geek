rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Anyone can READ the gallery (public portfolio)
    match /gallery/{docId} {
      allow read: if true;
      // Only authenticated users can write (you, via Firebase Console)
      allow write: if request.auth != null;
    }
  }
}
